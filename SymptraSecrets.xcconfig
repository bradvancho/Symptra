// Committed wrapper: pulls in local secrets if present.
// Copy Config/Secrets.xcconfig.example → Config/Secrets.xcconfig and fill in IMH credentials.
// Default empty namespace; overridden when Config/Secrets.xcconfig sets IMH_NAMESPACE_ROOT.
IMH_NAMESPACE_ROOT =
#include? "Config/Secrets.xcconfig"
